# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#                                                            // foundry // spago
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#
# SMART Brand Ingestion Engine - PureScript Schema Layer
#
# This package defines the typed Brand schemas that:
#   1. Are proven correct in Lean4 (lean/Foundry/Brand/*)
#   2. Are consumed by the Haskell extraction pipeline (foundry-*)
#   3. Are rendered by agents for brand-consistent UI generation
#
# Build: spago build
# Test:  spago test
# REPL:  spago repl
#
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

package:
  name: foundry-brand
  dependencies:
    - arrays
    - effect
    - integers
    - maybe
    - newtype
    - numbers
    - prelude
    - strings
  build:
    strict: true

workspace:
  packageSet:
    registry: 73.3.0
  extraPackages: {}
